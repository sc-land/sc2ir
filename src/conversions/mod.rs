/*!
 * Conversions module for main SC to IR transformations
 *
 * This module contains implementations of the main conversion traits:
 * - `ir`: Implementation of `IRFromSC` for converting the full SC Tree to IR
 * - `larvie`: Implementation of `LarvieFromBug` for converting Bug to Larvie
 */

pub mod ir;
pub mod larvie;
